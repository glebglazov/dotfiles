# Theme (must be loaded first for instant prompt)
romkatv/powerlevel10k

# Core functionality
zsh-users/zsh-syntax-highlighting
zsh-users/zsh-autosuggestions
zsh-users/zsh-completions

# Common aliases and utilities
# plugins/common-aliases deliberately omitted: it claimed short names we want
# (`t`), shipped global aliases that expand mid-line (`T`, `G`, `H`), and mapped
# file suffixes to X11-era binaries we don't have. The few aliases we used from
# it are defined in .my_zsh_plugins/setup_aliases_and_commands.sh instead.
ohmyzsh/ohmyzsh path:lib/directories.zsh
